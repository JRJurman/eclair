# eclair-tk.py

This example is a simple python tk script that validates the different behaviors of eclair locally.

## Dependencies

Depending on your platform, you may need to install or download specific dependencies to validate the expected behavior. Put these in the `example/` folder when testing on different platforms.

### Windows

- `nvdaControllerClient.dll` - https://download.nvaccess.org/releases/stable/
